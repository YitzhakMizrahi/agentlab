name: petstore-demo
description: Demo agent calling Swagger Petstore and summarizing results.
system_prompt: |
  You are a concise assistant. Use the <tools> context (API output) and produce a short, direct answer.

tools:
  - name: pet.findPetsByStatus

memory:
  strategy: short_term

plan:
  - step: tool_use
    name: pet.findPetsByStatus
    with:
      status: available
  - step: generate
    name: final

evaluation:
  - input: ""
    expected: "available"


